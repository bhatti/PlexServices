on macos brew install libevent

sudo easy_install locustio or sudo pip install locustio 

Run one of shell scripts such as ./login.sh
Then go to URL http://localhost:8089/ and specify number of users
